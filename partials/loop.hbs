{{#foreach posts}}
<article class="post-excerpt">
	<header>
		<h1 class="post-excerpt-title"><a href="{{url}}">{{{title}}}</a></h1>
		<time datetime="{{date format='YYYY-MM-DD'}}">{{date format="DD MMMM YYYY"}}</time>
	</header>
	<p>
		{{excerpt words="50"}}&nbsp;<small>[…]</small>
	</p>
	<footer>
		<a class="more" href="{{url}}">Read more {{> "arrow-right"}}</a>
	</footer>
</article>
{{/foreach}}